import { type NextPage } from "next";
import Layout from "~/components/layout";

type Invities = object;

const Invities: NextPage<Invities> = (props) => {
  return <Layout></Layout>;
};

export default Invities;
